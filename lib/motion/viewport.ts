// Shared viewport configuration for scroll reveal triggers across all sections
export const defaultViewport = {
  once: true,
  margin: "-40px",
  amount: 0.15,
};

export const heroViewport = {
  once: true,
  amount: 0.05,
};
